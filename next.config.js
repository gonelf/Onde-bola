/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // URL rewrites (ported from vercel.json) are added in Phase 5, once the
  // corresponding App Router routes and API handlers exist. Keeping this
  // minimal during the scaffold phase.
};

module.exports = nextConfig;
